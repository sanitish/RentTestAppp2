<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rent.rentmanagement.renttest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation|screenSize"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".BuildActivity" />
        <activity android:name=".roomActivity" />

        <supports-screens
            android:anyDensity="true"
            android:largeScreens="true"
            android:normalScreens="true"
            android:smallScreens="true"
            android:xlargeScreens="true" />

        <activity android:name=".roomDetailActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".TotalTenantsctivity" />
        <activity android:name=".edit_rooms" />
        <activity android:name=".studentProfile" />
        <activity android:name=".MainActivity" />
        <activity android:name=".automanualActivity" />
        <activity android:name=".manualActivity" />
        <activity android:name=".buildingActivity" />
    </application>

</manifest>